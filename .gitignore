/target
**/*.rs.bk
Cargo.lock
.env
config.toml

# Ad-hoc debug fixtures (raw tx / signature dumps)
/fomo*.json
/tx*.json
/nul

# Runtime state (route shards + plan file). Regenerated at boot.
/route_shards.json
/route_shards.plan.json
